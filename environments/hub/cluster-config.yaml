apiVersion: v1
kind: ConfigMap
metadata:
  name: cluster-config
  namespace: argocd
  labels:
    environment: hub
data:
  # Cluster identification
  environment: "hub"
  provider: "aws"
  cluster_name: "hub-eks"

  # AWS configuration
  region: "us-west-2"
