# Grafana Operator - hub overrides (base config; single replica is fine for the
# hub). Reconciles the GrafanaDashboard/GrafanaDatasource CRs onto the hub's
# external Amazon Managed Grafana workspace.
