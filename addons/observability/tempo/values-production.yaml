# Tempo - Production overrides
# Production: longer retention, larger storage

serviceAccount:
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::000000000000:role/production-eks-tempo

tempo:
  retention: 720h

persistence:
  size: 100Gi
