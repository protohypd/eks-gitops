# Argo Workflows - Production overrides

server:
  serviceAccount:
    annotations:
      eks.amazonaws.com/role-arn: arn:aws:iam::000000000000:role/production-eks-argo-workflows

artifactRepository:
  s3:
    bucket: aws-eks-production-argo-artifacts
