# Argo Workflows - Staging overrides

server:
  serviceAccount:
    annotations:
      eks.amazonaws.com/role-arn: arn:aws:iam::000000000000:role/staging-eks-argo-workflows

artifactRepository:
  s3:
    bucket: aws-eks-staging-argo-artifacts
