# cert-manager - Production overrides

replicaCount: 3

serviceAccount:
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::000000000000:role/production-eks-cert-manager

webhook:
  replicaCount: 3

cainjector:
  replicaCount: 3
