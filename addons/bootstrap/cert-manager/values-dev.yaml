# cert-manager - Dev overrides

replicaCount: 1

serviceAccount:
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::000000000000:role/dev-eks-cert-manager

podDisruptionBudget:
  enabled: false

webhook:
  replicaCount: 1

cainjector:
  replicaCount: 1
