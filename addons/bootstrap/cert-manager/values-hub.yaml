# cert-manager - hub overrides. Single replica for the hub control plane.

replicaCount: 1

serviceAccount:
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::000000000000:role/hub-eks-cert-manager

webhook:
  replicaCount: 1

cainjector:
  replicaCount: 1
