apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: addons-operations-helm
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "40"
spec:
  goTemplate: true
  goTemplateOptions: ["missingkey=error"]
  generators:
    - matrix:
        generators:
          - clusters:
              selector:
                matchLabels:
                  argocd.argoproj.io/secret-type: cluster
          - list:
              elements:
                - appName: velero
                  namespace: velero
                  chartRepo: https://vmware-tanzu.github.io/helm-charts
                  chart: velero
                  chartVersion: 12.0.2
                  path: addons/operations/velero
                  syncWave: "40"
                - appName: vpa
                  namespace: vpa
                  chartRepo: https://charts.fairwinds.com/stable
                  chart: vpa
                  chartVersion: "4.11.0"
                  path: addons/operations/vpa
                  syncWave: "41"
                - appName: goldilocks
                  namespace: goldilocks
                  chartRepo: https://charts.fairwinds.com/stable
                  chart: goldilocks
                  chartVersion: 10.3.0
                  path: addons/operations/goldilocks
                  syncWave: "42"
                - appName: descheduler
                  namespace: kube-system
                  chartRepo: https://kubernetes-sigs.github.io/descheduler/
                  chart: descheduler
                  chartVersion: "0.36.0"
                  path: addons/operations/descheduler
                  syncWave: "43"
                # karpenter is catalogued under operations but deploys at wave 5 —
                # right after the CNI (networking, wave 1) and before security (10),
                # observability, and tenant workloads, so the autoscaler is ready to
                # provision nodes for everything that follows. Its peers in this
                # ApplicationSet run at their nominal operations waves (40+).
                - appName: karpenter
                  namespace: kube-system
                  chartRepo: oci://public.ecr.aws/karpenter/karpenter
                  chart: karpenter
                  chartVersion: "1.8.6"
                  path: addons/operations/karpenter
                  syncWave: "5"
                - appName: keda
                  namespace: keda
                  chartRepo: https://kedacore.github.io/charts
                  chart: keda
                  chartVersion: "2.20.0"
                  path: addons/operations/keda
                  syncWave: "44"
  template:
    metadata:
      name: '{{ .appName }}'
      annotations:
        argocd.argoproj.io/sync-wave: '{{ .syncWave }}'
    spec:
      project: platform
      sources:
        - repoURL: '{{ .chartRepo }}'
          chart: '{{ .chart }}'
          targetRevision: '{{ .chartVersion }}'
          helm:
            releaseName: '{{ .appName }}'
            parameters:
              - name: clusterName
                value: '{{ index .metadata.labels "cluster_name" }}'
              - name: vpcId
                value: '{{ index .metadata.labels "vpc_id" }}'
            valueFiles:
              - $values/{{ .path }}/values.yaml
              - $values/{{ .path }}/values-{{ index .metadata.labels "environment" }}.yaml
        - repoURL: https://github.com/nanohype/eks-gitops.git
          targetRevision: main
          ref: values
      destination:
        server: https://kubernetes.default.svc
        namespace: '{{ .namespace }}'
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
          - ServerSideApply=true
        retry:
          limit: 5
          backoff:
            duration: 5s
            factor: 2
            maxDuration: 3m
